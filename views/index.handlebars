<h1>Burgers!</h1>

<h2>Burgers that are not devoured!</h2>

<ul>
    {{#each burgers}} {{#unless devoured}} {{> bugers/burger-block devoured=true}} {{/unless}} {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
    {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=false}} {{/if}} {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bu">Buger Name:</label>
        <input type="text" id="bu" name="name">
    </div>

    <div class="form-group">
        <label for="dev">Devoured?</label>
        <br>
        <input type="radio" name="dev" value="1" checked> Devoured
        <br>
        <input type="radio" name="dev" value="0">
    </div>

    <button type="submit">Add Burger</button>
</form>